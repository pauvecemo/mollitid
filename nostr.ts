// Define an interface for the structure
interface Person {
  name: string;
}

// Create an object that conforms to the Person interface
const Riyad: Person = {
  name: 'Riyad Mahrez', // Assuming the full name for the example
};

// Log the name of the person
console.log(Riyad.name);
